import math
import pandas as pd
import numpy as np
from numpy.random import rand
import matplotlib.pyplot as plt


def descend (p, g, t, x_batch y_batch, e):
	"""[5 points---my code for this was only three lines long!] Return p after performing t rounds of gradient descent with gradient g
	
	Keyword arguments:
	p -- a list of initialized parameters
	g -- gradient function. Takes a list of parameters, a matrix of data, and a vector of labels are returns an array of gradients, one for each parameter
	t -- the number of gradient steps
	x_batch -- batch of input data
	y_batch -- batch of labels
	e -- the learning rate; may be ignored
	""" 

	

	
